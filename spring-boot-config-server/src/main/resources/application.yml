spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://github.com/lyl-gis/spring-boot-practice/
          search-paths: spring-boot-config-repo
          username: username
          password: password
server:
  port: 7001